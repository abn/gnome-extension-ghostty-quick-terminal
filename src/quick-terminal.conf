# Loaded last through --config-file, so a user's own config-file imports
# cannot undo it. Everything else comes from the user's config.
window-decoration = none
gtk-titlebar = false
window-save-state = never
quit-after-last-window-closed = true
